version: '3.8'

# 🔥 1. 공통 설정 (OS 상관없이 뼈대가 되는 설정)
x-base-config: &base-config
  build: 
    context: .
    dockerfile: Dockerfile
    args:
      - DOCKER_MODE=${DOCKER_MODE:-server}
  image: lucia_project_env:${DOCKER_MODE:-server}
  container_name: lucia_${DOCKER_MODE:-server}
  stdin_open: true
  tty: true
  shm_size: 64g
  ulimits:
    memlock:
      soft: -1
      hard: -1
    stack:
      soft: 67108864
      hard: 67108864
  ports:
    - "3545:3545"
    - "3546:3546"
    - "8888:8888"
  working_dir: /workspace

services:
  # 🐧 2. 리눅스(Linux) 전용 환경
  melissa_linux:
    <<: *base-config
    profiles: ["linux"]
    runtime: nvidia
    ipc: host
    volumes:
      - './project_lucia_docker:/workspace'
      - './nltk_data:/workspace/nltk_data'
      - '/tmp/.X11-unix:/tmp/.X11-unix:rw'
      - '/sys/class/drm:/sys/class/drm:ro'
      - '/sys/devices:/sys/devices:ro'
      - '/sys/bus/pci:/sys/bus/pci:ro'
    environment:
      CUDA_HOME: /usr/local/cuda
      PATH: /usr/local/cuda/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
      LD_LIBRARY_PATH: /usr/local/cuda/lib64:/usr/local/lib:/usr/lib
      DISPLAY: ${DISPLAY}
      QT_X11_NO_MITSHM: "1"
      NVIDIA_VISIBLE_DEVICES: "all"
      NVIDIA_DRIVER_CAPABILITIES: "compute,utility,graphics"
      NLTK_DATA: /workspace/nltk_data
    devices:
      - "/dev/dri:/dev/dri"

  # 🪟 3. 윈도우(Windows / WSL2) 전용 환경
  melissa_windows:
    <<: *base-config
    profiles: ["windows"]
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    volumes:
      - './project_lucia_docker:/workspace'
      - './nltk_data:/workspace/nltk_data'
      - '/tmp/.X11-unix:/tmp/.X11-unix:rw'
      - '/mnt/wslg:/mnt/wslg'                     # 🔥 윈도우 내장 WSLg 그래픽 경로 마운트
    environment:
      CUDA_HOME: /usr/local/cuda
      PATH: /usr/local/cuda/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
      LD_LIBRARY_PATH: /usr/local/cuda/lib64:/usr/local/lib:/usr/lib
      DISPLAY: :0                                # 🔥 윈도우 호스트(파워쉘) 환경용 디스플레이 고정
      WAYLAND_DISPLAY: wayland-0                 # 🔥 WSLg 웨일랜드 활성화
      XDG_RUNTIME_DIR: /mnt/wslg/runtime-dir     # 🔥 GUI 런타임 공유
      QT_X11_NO_MITSHM: "1"
      NVIDIA_VISIBLE_DEVICES: "all"
      NVIDIA_DRIVER_CAPABILITIES: "compute,utility,graphics"
      NLTK_DATA: /workspace/nltk_data
    # ❌ 이곳에는 절대 devices: - "/dev/dri:/dev/dri" 가 들어가면 안 됩니다!
